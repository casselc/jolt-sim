{:paths ["src" "resources"]

 :deps
 {io.github.casselc/jolt-sim-report
  {:local/root "../report"}

  io.github.casselc/jolt-http
  {:git/url "https://github.com/casselc/jolt-http.git"
   :git/sha "96bd0fc93f1a2c77128d06f74802dd2a4631c0d4"}

  ;; Override jolt-http's older transitive transport pins with the reviewed
  ;; Jolt v0.7.3-compatible pair exercised by the canonical application aliases
  ;; in the repository root.
  io.github.casselc/jolt-tcp
  {:git/url "https://github.com/casselc/jolt-tcp.git"
   :git/sha "0ceaa900bfca11933d35831d7697c7e2c5b22f04"}

  io.github.casselc/jolt-net
  {:git/url "https://github.com/casselc/jolt-net.git"
   :git/sha "f335b8d5f26722233fd82af5e8c0e800397851c9"}

  ;; JSON wire encoding for /api/replay-progress. data.json loads
  ;; DateTimeFormatter support when its namespace initializes, so the current
  ;; upstream time provider is direct rather than an accidental transitive
  ;; detail, matching the root repository's outbox JSON facade aliases.
  io.github.casselc/data.json
  {:git/url "https://github.com/casselc/data.json.git"
   :git/sha "8a6dc9668e5c3596a335759defeb7ec80cd3b5f8"}

  io.github.jolt-lang/time
  {:git/url "https://github.com/jolt-lang/time.git"
   :git/sha "da271ca302487c2c5130a0450b4ad4c78d63e656"}}

 :aliases
 {:test
  {:extra-paths ["test"]
   :main-opts ["-m" "jolt.sim.viewer-test"]}

  ;; Deterministic real-handler fixture for Playwright. The injected replay
  ;; service writes one real retained activity journal and never launches the
  ;; expensive whole-application worker exercised by :e2e-test below.
  :browser-test-server
  {:extra-paths ["test"]
   :main-opts ["-m" "jolt.sim.viewer-browser-server"]}

  :e2e-test
  {:extra-paths ["test"]
   :main-opts ["-m" "jolt.sim.viewer-replay-e2e-test"]}

  :remote-session-producer
  {:extra-paths ["test"]
   :main-opts ["-m" "jolt.sim.viewer-remote-session-producer"]}

  :remote-session-consumer
  {:extra-paths ["test"]
   :main-opts ["-m" "jolt.sim.viewer-remote-session-consumer"]}

  :viewer
  {:main-opts ["-m" "jolt.sim.viewer"]}}}
